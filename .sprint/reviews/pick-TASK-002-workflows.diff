diff --git a/.github/workflows/daily_screening_git_storage.yml b/.github/workflows/daily_screening_git_storage.yml
index 8f1492e0..30243ff0 100644
--- a/.github/workflows/daily_screening_git_storage.yml
+++ b/.github/workflows/daily_screening_git_storage.yml
@@ -18,6 +18,10 @@ on:
         type: boolean
         default: false
 
+concurrency:
+  group: screening-git-storage
+  cancel-in-progress: false
+
 jobs:
   screen-stocks:
     runs-on: ubuntu-latest
@@ -108,7 +112,7 @@ jobs:
           # pool, then filters/ranks that pool down to a Top 5 shortlist in the
           # email. Fundamentals Auditor/Catalyst Sentiment no-op with a logged
           # warning if ANTHROPIC_API_KEY isn't set as a repo secret.
-          python run_optimized_scan.py --conservative --git-storage --enable-llm-agents
+          python run_optimized_scan.py --conservative --git-storage --enable-llm-agents --run-kind daily-full
 
       - name: Show fundamental refresh stats
         if: always()
@@ -141,6 +145,11 @@ jobs:
             echo "No metadata file found"
           fi
 
+      - name: Stage pick history
+        continue-on-error: true
+        run: |
+          if [ -d data/pick_history ]; then git add data/pick_history/; fi
+
       - name: Commit updated fundamental cache
         id: commit
         run: |
diff --git a/.github/workflows/midday_quick_scan.yml b/.github/workflows/midday_quick_scan.yml
index 925399d0..b19ef8a1 100644
--- a/.github/workflows/midday_quick_scan.yml
+++ b/.github/workflows/midday_quick_scan.yml
@@ -9,6 +9,10 @@ on:
 
   workflow_dispatch: {}
 
+concurrency:
+  group: screening-git-storage
+  cancel-in-progress: false
+
 jobs:
   quick-scan:
     runs-on: ubuntu-latest
@@ -46,7 +50,7 @@ jobs:
         run: |
           mkdir -p data/daily_scans
           mkdir -p data/logs
-          python run_optimized_scan.py --test-mode --git-storage
+          python run_optimized_scan.py --test-mode --git-storage --run-kind midday-sample
 
       - name: Commit updated fundamental cache
         id: commit
diff --git a/.gitignore b/.gitignore
index 2c2a6380..8bdff5e4 100644
--- a/.gitignore
+++ b/.gitignore
@@ -53,3 +53,6 @@ data/*
 !data/catalyst_sentiment/**
 !data/congress_trades/
 !data/congress_trades/**
+!data/pick_history/
+!data/pick_history/**
+data/pick_history/**/.*.tmp
