TASK MR-002 - Market view: page-level section tabs (market.js)

Read first: .sprint/SPRINT_PLAN_MARKET_REORG.md (full spec, converged decisions, disagreement resolutions - final).
static/css/dashboard.css already has .market-section-tabs/.market-section-tab/.market-section-panel[hidden]/
.market-motion-table-disclosure (MR-001, done - use those classes, don't add new ones without a reason).

FILES YOU MAY TOUCH: static/js/views/market.js only. Do NOT touch static/js/components/market-map.js (another
engineer, MR-003, owns it in parallel right now - if you need a new method on the map controller, e.g.
setActive()/suspend()/resume(), assume it will exist with that exact name/signature per the plan and call it;
list any assumption in your report so the lead can reconcile).

GOAL (from the plan, final): wrap the EXISTING render output of loadMarketScan() into 4 tab panels, without
changing what any of them render or removing any existing id/export:
1. "Buy Opportunity Map" (default tab) - the #marketMotionMount card exactly as it exists today.
2. "Overview" - Market Breadth donut (#breadthChart) + SPY Regime card (the current .grid-2 block) + the news
   sections (#newsSections and its children) moved here, unchanged internally.
3. "Top 20" - the renderTop20Table(...) output, unchanged.
4. "Signals" - the EXISTING #marketSignalsCard (_signalTabsHTML output) unchanged, including its own nested
   Buy/Sell tablist (_activateSignalTab, _wireSignalTabs, _showBuySignal) - do not touch that code at all, just
   wrap its existing container in the new "Signals" panel.

Always visible ABOVE the tabs (unchanged position): the source note, scan selector, and the 5-tile stats row.

Add `_activateMarketSection(sectionId)`: updates .market-section-tab .active/aria-selected/tabindex, toggles
.market-section-panel[hidden], persists to `sessionStorage.setItem('market:section', sectionId)` (read on
initial render via `sessionStorage.getItem('market:section') || 'map'`; reset to 'map' if the stored value isn't
one of the 4 valid ids). Wire roving-tabindex + ArrowLeft/ArrowRight/Home/End exactly like the existing
`_wireSignalTabs` pattern in this same file (read it first, copy its shape, don't reinvent).

Lazy-mount the map: call `mountMarketMap()` (or whatever the current mount call is named - grep for it) ONLY the
first time the "map" section is actually activated (which may be immediately, since it's the default tab, OR
later if sessionStorage restored a different tab). If the restored section is NOT "map", do not fetch/build the
map at all until the user clicks that tab. Once mounted, never unmount/rebuild it on subsequent tab switches -
call the controller's setActive(true)/setActive(false) (from MR-003) when switching into/out of the map tab
instead.

The existing 90-second Live poll (grep for it - `_liveGuarded`/`_startLive` or similar) must keep running for ALL
sections regardless of which tab is active - do not gate it behind the map tab being open. It should call the map
controller's lightweight poll method (pollLatest or similar) if the controller exists, so "New snapshot available"
stays honest even while looking at another tab.

Keep every existing DOM id/class/export used by tests/js/test_market_*.js: grep those test files first for exact
ids/selectors/exported function names and preserve every one of them.

TESTS: run the FULL tests/js/*.js suite (`for f in tests/js/*.js; do node "$f" || echo "FAIL $f"; done`) and
confirm zero new failures. Add `tests/js/test_market_sections.js` covering: default section is "map", tab
switching updates aria-selected/hidden correctly, ArrowRight/ArrowLeft/Home/End keyboard nav, sessionStorage
persists and restores (including an invalid stored value falling back to "map"), the map is NOT mounted/fetched
when the initial/restored section is not "map", and mounting happens exactly once even after switching tabs
back and forth. Use the existing DOM-stub/mock style already in tests/js/test_market_*.js files (fetch mocks,
document stubs) - don't invent a new testing style.

No emoji. Never reference position/. No git mutation. Report to .sprint/results/mr-TASK-002-codex.md (concise:
files changed, decisions, any assumption about market-map.js's new methods, test results).
