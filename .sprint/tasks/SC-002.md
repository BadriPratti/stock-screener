TASK SC-002 - Expose every scoring sub-component in `details` (genuinely behavior-neutral)

Read .sprint/SPRINT_PLAN_SCORING.md fully first (Amendment 1 corrects an earlier mistake: cliff-smoothing is a
SEPARATE later task, SC-003 - this task must NOT change any score, only expose more detail about how it was
computed).

HARD constraint, the most important thing about this task: after your change, for every single ticker/input,
`score_buy_signal(...)`'s returned `score`, `is_buy`, `entry_quality`, `stop_loss`, `risk_reward_ratio`, and every
existing key in `details` must be BIT-FOR-BIT IDENTICAL to before your change. You are only ADDING new keys to
the `details` dict that don't exist yet, never modifying how any existing value is computed. If any existing test
in tests/test_signal_engine.py (or wherever the scoring tests live - find them) would need its expected `score`
value changed because of your edit, you have broken this constraint - stop and reconsider, don't change the test.

FILES YOU MAY TOUCH: src/screening/signal_engine.py only, plus a NEW test file tests/test_signal_engine_details.py
(or extend the existing scoring test file if one covers `details` already - check first, e.g. grep for
"details\[" or "trend_score" in tests/). Do not touch any other file.

WHAT TO EXPOSE (read the real code at the line ranges below - they may have shifted slightly, use them as a
starting point not gospel):
- Inside the trend-score section (currently ~signal_engine.py:188-253): the raw `distance_component` (before the
  min/max clamp AND after), `slope_component` (before/after), whether a breakout fired and which `breakout_type`,
  the raw extension-penalty amount actually subtracted (0, -5, or -10) as its own signed field, and
  `stage2_quality` (distance+slope combined, pre-breakout/pre-penalty).
- Inside entry-score (~signal_engine.py:563-644): the raw `sma_score`/SMA-proximity subpiece and the raw
  52-week-high-proximity subpiece, separately, both before and after their respective multipliers.
- Inside fundamentals (~signal_engine.py:255-388): whether it took the "no fundamentals" flat-17.5 branch or the
  real-data branch, and if real-data, the raw (unscaled) revenue/EPS/inventory/margin-placeholder sub-values
  separately, plus a `fundamentals_missing` boolean flag distinguishing "no dict at all" from "dict present but a
  specific sub-field missing" (Antigravity's finding: missingness currently looks identical to a real neutral
  score - make it distinguishable in the exposed data, without changing what score it still produces today).
- volume_score's raw pre-scale value and the `vol_ratio` it was computed from (already partially in `details` -
  check what's there, only add what's missing).
- rs_score, rr_score, vcp_bonus: expose their raw pre-scale values the same way if not already present.
- A single new top-level field, e.g. `details['score_version'] = 'v1-instrumented'` or similar, so this
  instrumented output can be told apart from the exact same formula's shape before this change if that ever
  matters downstream.

TESTS (this is the real acceptance bar, make it rigorous, not superficial):
1. A "golden" regression test: construct (or reuse existing fixtures for) a representative BATCH of realistic
   inputs spanning different phases/distances/slopes/fundamentals-present/fundamentals-absent/breakout/
   over-extended cases. Before your change, capture score_buy_signal's full output for each (you can do this by
   checking out/stashing nothing - just note the CURRENT behavior is the baseline you must match; if a pre-change
   snapshot mechanism already exists in this repo's test style, use it). After your change, assert every existing
   field (score, is_buy, entry_quality, stop_loss, risk_reward_ratio, and every key that existed in `details`
   before) is unchanged, for every case in the batch.
2. Tests that the new sub-component fields are actually present, correctly signed, and sum back to their parent's
   final value (e.g. distance_component + slope_component + breakout_bonus + extension_penalty, after the 35/40
   scale, equals `details['trend_score']`) - this is what makes the instrumentation TRUSTWORTHY for the later
   correlation study (SC-006), not just decorative.
3. The fundamentals-missing-vs-present distinction test: a signal with fundamentals=None and a signal with
   fundamentals={} (present but empty) must now be distinguishable via the new field, while producing the exact
   same fundamental_score as before (17.5 either way, per the current code - don't change that, just make it
   observable which path was taken).
4. Run the FULL existing test suite for signal_engine.py and anything that imports it (grep for
   "from src.screening.signal_engine import" or "score_buy_signal" across tests/ and run all of those files) and
   confirm zero failures/changes.

No emoji. Never reference position/. No git mutation. Report to .sprint/results/sc-TASK-002-codex.md: exact list
of new `details` keys added, confirmation (with the specific test names) that behavior is unchanged, and full
test results.
