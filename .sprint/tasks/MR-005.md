TASK MR-005 - Reconcile tests + full regression for the Market reorg (MR-002/003/004)

Read .sprint/SPRINT_PLAN_MARKET_REORG.md for context. MR-002, MR-003, MR-004 are all complete and merged into
static/js/views/market.js, static/js/components/market-map.js. The only known failure right now:

tests/js/test_market_map_component.js asserts the tracked-stock table's rows/sortable headers exist in the
INITIAL marketMapHTML() output. That's no longer true: MR-003 made the table a closed-by-default disclosure
(id="marketMotionTableDisclosure", aria-expanded="false", a hidden #marketMotionTableContainer) that only builds
rows once opened. Fix ONLY this: update the test's assertions to (a) check the closed disclosure shows the
correct row count in its own text ("Tracked stocks (N)") and the table container is hidden/empty initially, then
(b) simulate opening the disclosure (however the component exposes that - check the actual controller/DOM API in
market-map.js, e.g. clicking the button / calling a method) and THEN assert the sortable headers and rows exist.
Do not weaken coverage - it should still verify everything it did before, just after opening the disclosure
instead of assuming eager rendering.

FILES YOU MAY TOUCH: tests/js/test_market_map_component.js only, plus you may ADD new test files under tests/js/
if you find additional gaps (see below), but must not touch any static/js/ production file - MR-002/003/004 are
done and reviewed, do not modify them.

Then: run the ENTIRE tests/js/*.js suite and confirm zero failures (`for f in tests/js/*.js; do node "$f" || echo
"FAIL $f"; done`). Also run: `venv/bin/python -m pytest tests/test_dashboard_jobs.py -q` and a real ES-module
graph load of static/js/dashboard.js (grep this repo's existing tests for the exact node --input-type=module
command pattern used elsewhere in tests/js/ or in past sprint verification and reuse it).

Additionally, review test_market_sections.js (written by MR-002) and any lifecycle tests MR-003 added for
completeness against the plan's stated acceptance criteria (roving tabindex + arrow/Home/End keys, sessionStorage
persistence including invalid-value fallback, single lazy mount, setActive pausing replay + closing Expand
overlay, chart.resize on reactivation without refetch, disclosure builds rows once). If something the plan
required is untested, add a test for it (in the most appropriate existing file) rather than assuming it's fine.

No emoji. Never reference position/. No git mutation. Report to .sprint/results/mr-TASK-005-codex.md: what you
changed, full suite result (exact pass count), any gap you found and filled, any risk for MR-006 (final
verification/browser pass) to specifically look at.
