TASK MM-008 - the persistent tracker map (frontend integration, sole writer)

You are the SOLE writer of frontend files. Read first: .sprint/SPRINT_PLAN_LIVE_CHART.md (main plan + Amendments 1 and 2: the "Frontend" section and Amendment 1 "Chart changes that follow from this" are the spec), static/js/core/market-motion.js and tests/js/test_market_motion.js (finished pure model: use its exports, do not rewrite it; if it needs a change, describe it in the report), the finished modal sprint (.sprint/results/modal-all-codex.md; reuse its overlay/focus-trap pattern for "Expand"), static/js/views/market.js, static/js/charts.js, static/css/dashboard.css, dashboard.py routes /api/market-motion, /api/market-motion/latest, /api/market-motion/tracker, /api/market-motion/scheduler, /api/market-motion/run-now (read-only; do not edit dashboard.py).

FILES YOU MAY TOUCH: static/js/views/market.js, static/js/charts.js, static/css/dashboard.css, static/js/dashboard.js, static/js/core/ui-helpers.js (only if needed), a NEW static/js/components/market-map.js (controller/DOM), new/updated tests under tests/js/. Not market-motion.js, not Python.

BUILD
1. Replace the Market view's "Buy Opportunity Map" card (buildBuyOpportunityPoints stays exported and tested; the OLD scatter is used as the fallback when /api/market-motion returns zero frames or the fetch fails, with a one-line explanation) with the persistent map: full content width, height clamp(560px, 72vh, 880px), fixed axis domain from chartDomain(), drawn threshold line at thresholdY (48%) labelled "Buy line", zero-RS line kept. ALL ACTIVE + WATCHING tracked stocks plotted (RETIRED behind a "Show retired" toggle). Encodings: colour = entry quality (existing palette), solid = active, hollow = watching, muted hollow = stale (held at last real position, tooltip shows stateLabel/dropReasonLabel), radius = radiusForStreak(current_streak_days) * item.scale, alpha from items. Tooltip: ticker, "N days in a row (longest M)", "qualified S of D sessions", first qualified, score, RS, entry quality, state, and (when stale) the last-real-position date. Click a dot: existing behaviour (jump to its Buy Signals row) when a row exists, otherwise focus/highlight its row in the tracked table.
2. Rendering: ONE Chart.js instance created once per canvas, updated in place. The motion is driven by market-motion.js itemsAt() at fractional t via a requestAnimationFrame loop from createPlayer(); per frame mutate the single dataset and call chart.update('none'). Custom Chart.js plugin (in charts.js, plain global function, NO export) draws the buy line, 1-step motion vectors during playback, 3-segment tails (trailPoints) only for hovered/selected tickers or when the plotted count <= 250, and labels for hovered/selected + top 10 by score + top 10 movers (cap 20). decimate() to DEFAULT_POINT_BUDGET; if the median frame time (measure with performance.now over a rolling 30 frames) exceeds 33 ms, suppress tails then labels BEFORE dropping any active point. _destroyChart must call a registered cleanup that cancels the rAF loop and removes the matchMedia listener, then destroy the chart.
3. Controls (real buttons/inputs, keyboard operable, aria-labels, no emoji): Play/Pause, Previous, Next, a range slider over recorded frames (step 0.01 continuous scrub), speed 0.5x/1x/2x, "Live" / "Return to live", a status line with frameLabel() of the shown frame plus elapsed gap, and the "New snapshot available" note when createPlayer reports newAvailable. aria-live="polite" region updated only when the displayed frame index changes. Honour prefers-reduced-motion (matchMedia + change listener): no autoplay, instant steps, tails on hover only; the CSS media query from the modal sprint already exists, reuse it.
4. Filters and search: chips for Active / Watching / Retired, Entry quality (Good/Extended/Poor), Streak >= 2 / >= 3 / >= 5 (single-select), and a ticker search box that highlights/selects the matching dot (use filterTickers()). Filtering must not restart the animation.
5. Tracked-stocks table under the map (buildTrackerRows/sortRows): sortable keyboard-accessible headers (mirror static/js/views/consistency.js), columns ticker, tier, streak, longest, qualified of observed, score, RS, entry quality, first qualified, score trend, state; it lists EVERY record including points the chart decimated, uses escapeHtml everywhere, and is the accessible alternative (give the canvas role="img" with an aria-label summary: counts of active/watching/stale, frame label).
6. Honest copy (plain text): heading "Buy Opportunity Map", subtitle stating "Tracking N stocks (A active, W watching). Intraday re-score: R of N." from /api/market-motion/tracker `roster` + `counts`, a coverage note when tracker.top50_only_sessions is non-empty ("Sessions <dates> recorded only the top 50 buys; other stocks are unknown for those days, not dropped."), "Intraday re-score, fundamentals as of <fundamentals_as_of>" when the newest frame is intraday. If a frame's scoring_version differs from its neighbour show a divider note.
7. Scheduler card (small, above the table): reads /api/market-motion/scheduler; shows message/last run/last publish/next run; a labelled toggle "Auto-refresh every 2 hours while this app is open" (POST {"enabled": bool}), a "Publish frames to GitHub" toggle (POST {"publish_to_github": bool}) and a "Run now" button (POST /api/market-motion/run-now, disable while running). It must show clearly that the switch is OFF by default and that nothing runs unless turned on. Errors are shown inline, never thrown.
8. Data flow: fetch /api/market-motion?limit=20 and /api/market-motion/tracker (default tiers active,watching; also request retired only when the toggle is on) with a per-view request token so a stale response can never paint (also apply the same token guard to loadMarketScan so switching the report dropdown quickly cannot paint an older scan last). The existing 90-second Live loop must poll /api/market-motion/latest and, on a new run_id, fetch the new frame and call player.setFrameCount so the map tweens in place (or shows "New snapshot available" if scrubbed back) instead of re-rendering the view. The app-wide 5-minute auto-sync must not reset the map: dispatch a window CustomEvent 'stock-data-updated' from dashboard.js after a successful pull and let Market handle it in place (refetch frames, keep the player position); keep the existing route() re-render behaviour for the other views.
9. Expand: an "Expand" button opens the map full-viewport using the modal sprint's overlay pattern (same z-index rules, focus trap, Escape); the single chart instance is reparented or recreated safely (destroy-then-recreate on the same canvas id rules apply; never "Canvas is already in use"); controls stay usable in expanded mode.
10. CSS in dashboard.css (append, matching existing tokens/spacing); nothing that breaks the 760px responsive rule; no emoji.

TESTS (plain Node, repo style, deterministic; inject clock/rAF; mock Chart if needed as existing tests do): controller state machine (filters, search, tier toggle, player wiring, reduced motion, stale-response token, live update follow vs scrubbed back), DOM/ARIA structure of controls/table/scheduler card, sort behaviour, table lists every record when decimation drops some, fallback to the old scatter when no frames, scheduler card actions (mock fetch: correct URLs/bodies), 'stock-data-updated' handling, _destroyChart cleanup cancels rAF and listeners (static/source assertion that charts.js has no `export`), no emoji in new source. Also a 2,000-point fixture performance smoke test (itemsAt + decimate over 2,000 tickers must complete a frame computation in well under 33 ms on average in Node; print the measured average; do not make it timing-flaky: assert a generous bound). Run ALL tests/js/*.js and the ES-module graph load check. Report to .sprint/results/mm-TASK-008-codex.md (under 70 lines).
