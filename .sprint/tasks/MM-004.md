TASK MM-004 - intraday re-score command (no LLM, no email, no canonical outputs)

FILES YOU MAY TOUCH (all NEW unless stated)
- run_intraday_rescore.py (repo root)
- src/screening/intraday_rescore.py (helpers)
- tests/test_intraday_rescore.py
Do NOT edit run_optimized_scan.py (another engineer owns it right now); import from src.* only. You may READ run_optimized_scan.py main() to copy the exact buy-scoring call shape, but do not import from run_optimized_scan (it has argparse side effects); duplicate the small scoring loop in your helper and note the duplication in your report so the lead can add a parity test.
Report path: .sprint/results/mm-TASK-004-codex.md

GOAL
A command that re-scores ONLY the tracked roster during market hours and writes one `intraday_rescore` market-motion frame. It must be cheap (roster <= 500 tickers, ~8-10 minutes at 2 workers / 1.0 s delay) and must not disturb anything else.

REQUIREMENTS
1. CLI: `python run_intraday_rescore.py [--cap 500] [--workers 2] [--delay 1.0] [--root PATH] [--dry-run] [--force]`. Defaults are the conservative pacing. `--root` overrides the market-motion root (tests/local). `--dry-run` computes and prints the summary but writes nothing. `--force` skips ONLY the market-open guard (manual testing on weekends) and must be loudly logged.
2. Flow: load frames with market_motion.load_frames(root); tracker = compute_tracker(frames); roster = build_roster(tracker, cap). If there are no tracked stocks: exit 0, no frame, status "skipped" reason "no_tracked_stocks (run a full scan first)". Otherwise create the OptimizedBatchProcessor (src/screening/optimized_batch_processor.py) with git storage ON (so cached fundamentals are used), call fetch_spy_data(); if unavailable exit 1 with a clear message. Market-open guard: `market_motion.is_data_current(<last index value of processor.spy_data>)` must be True unless --force; otherwise exit 0, no frame, status "skipped" reason "market_closed_or_no_bar_today". session_date = market_motion.session_date_from_bar(<last SPY bar>).
3. Analysis: process ONLY the roster tickers via processor.process_batch_parallel(roster_tickers, resume=False, min_price=5.0, min_volume=100000) - same filters as the daily scan. INVESTIGATE process_batch_parallel/save_progress/load_progress in src/screening/optimized_batch_processor.py: it must not read or overwrite the daily scan's progress file, or any other shared state; if it does, isolate it (e.g. point its progress file at a temp path) WITHOUT editing the processor file, and say what you did. Then for each analysis: phase in (1, 2) -> score_buy_signal(...) exactly as run_optimized_scan.main does (copy its argument shape) ; is_buy -> market_motion.normalize_buy_signal(sig, rank) (set signal['current_price'] like main does); not a buy -> market_motion.outcome_point(ticker, signal=sig, phase=...); phase 3/4 -> outcome_point(ticker, phase=phase); roster tickers with no analysis -> outcome_point(ticker, error="not_analyzed"). Ranks are by score desc among qualified. Roster overflow tickers (tracked ACTIVE/WATCHING beyond the cap) get outcome_point(ticker) (= not_evaluated) rows so it is explicit they were not re-scored.
4. Regime: inherit from the newest completed daily frame: frame regime = {"should_generate_buys": <inherited>, "source_run_id": <that daily frame run_id>}. If the inherited should_generate_buys is False, a would-be buy is recorded as not_qualified with drop_reason "regime_gate" (coordinates kept). Never recompute breadth. fundamentals_as_of = the newest daily frame's session_date.
5. Frame: market_motion.build_frame(run_kind=RUN_KIND_INTRADAY, provenance "live", scope={"mode": "tracked_roster", "requested": len(roster), "analyzed": a, "failed": f, "completed": True, "roster_cap": cap, "overflow": n, "candidate_source_run_id": <newest daily run_id>}), coverage complete_for_scope, then market_motion.write_frame(frame, root). Write ONLY after the whole roster finished; on any exception write nothing and exit 1. If analyzed == 0 the run is broken: write nothing, exit 1.
6. Must NOT: send any email, call any LLM/agent, fetch Reddit/insider/Congress/earnings/news data, write any report/top20/shortlist/latest file, write pick-history, or write anything except the single frame file (verify by listing the data directory before/after in a test). No `print` of secrets.
7. Last stdout line is machine-readable for the scheduler: `MARKET_MOTION_RESULT {json}` where json has status ("written"|"skipped"|"failed"), reason, run_id (or null), session_date, roster, analyzed, elapsed_seconds. Exit codes: 0 written/skipped, 1 failed.
8. Tests (offline; stub the processor/SPY/scoring via dependency injection - structure the code as a pure `run_rescore(processor_factory, score_fn, frames_root, cap, now, force, dry_run)` core with the CLI as a thin wrapper): guard skip on weekend data, --force override, no tracked stocks skip, roster ordering and cap + overflow rows, each outcome state, regime_gate downgrade, analyzed==0 -> failure and no file, exception mid-run -> no file, dry-run writes nothing, only one file written, result line format, market_motion tracker afterwards sees the new frame (position moves, current_streak_days unchanged, current_streak_frames increments).
