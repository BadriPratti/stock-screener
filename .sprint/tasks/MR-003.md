TASK MR-003 - Market map controller lifecycle: setActive/suspend/resume + lazy tracked-table disclosure

Read first: .sprint/SPRINT_PLAN_MARKET_REORG.md (full spec - final). static/css/dashboard.css already has
.market-motion-table-disclosure styling (MR-001, done).

FILES YOU MAY TOUCH: static/js/components/market-map.js only. Do NOT touch static/js/views/market.js (another
engineer, MR-002, owns it in parallel right now and will call the methods you add here by the names below).

GOAL: the map's controller (whatever mountMarketMap/its returned object is called - read the file first) needs
two additions, both behavior-preserving for the CURRENT single-panel usage (i.e. do not change how it behaves
today when nothing calls the new methods):

1. `setActive(isActive)` (exact name) on the controller object mountMarketMap() returns:
   - setActive(false): if the replay player is mid-playback, pause it (call whatever the Play/Pause button's own
     handler calls - reuse it, don't duplicate pause logic). Close the Expand overlay if it's open. Do NOT cancel
     the 90-second live poll (that's driven externally by market.js per MR-002 - if THIS file currently owns/starts
     its own poll timer internally, expose a separate lightweight method for market.js to call instead, e.g.
     pollLatest(), and describe what you found in your report).
   - setActive(true): call `chart.resize()` (grep charts.js for how the Expand overlay already does this exact
     resize-on-reveal pattern and reuse the same approach) then a single repaint at the current replay position -
     do NOT resume playback automatically, do NOT refetch frames/tracker unless the poll detected a newer frame
     while inactive (in which case mark state dirty and do the normal "New snapshot available" flow, don't force
     a silent refresh).
   - Must never destroy/recreate the Chart.js instance in either direction - this is the exact "Canvas is already
     in use" trap this repo has hit before (see charts.js's existing _destroyChart pattern for why).

2. Tracked-stocks table: wrap the existing table in a closed-by-default disclosure using the
   `.market-motion-table-disclosure` class from dashboard.css - a `<button aria-expanded="false"
   aria-controls="...">Tracked stocks (N)<span class="chevron">...</span></button>` followed by the table
   container (`hidden` while closed). The row count N always reflects the current filtered/tiered count even
   while closed. Build the actual `<tr>` HTML only the FIRST time the disclosure opens (not on every render while
   it's open, and not while it's closed) - subsequent sort/filter changes while it's already open should still
   update it live as they do today; while closed, mark it dirty and rebuild once on next open instead of eagerly
   rendering rows nobody can see. Toggling the disclosure must not affect the chart/replay/filters at all.

Preserve every existing exported function name, DOM id, and class the current file has (grep test_market_map_*.js
and test_market_motion_chart_renderer.js first for exactly what they depend on).

TESTS: run tests/js/test_market_map_controller.js, test_market_map_component.js, test_market_motion.js,
test_market_motion_chart_renderer.js and confirm they still pass, updating ONLY the parts of
test_market_map_component.js that currently assert every tracked row exists in the initial HTML (change to:
disclosure closed + count visible initially, rows present after simulating an open). Add new assertions/tests in
the same files (or a new tests/js/test_market_map_lifecycle.js) for: setActive(false) pauses an in-progress
replay and closes an open Expand overlay; setActid(true) resizes+repaints without resuming playback and without
refetching; setActive never causes a second Chart instance on the same canvas id (assert _destroyChart/create
call counts); disclosure builds rows once on first open, not before, and reflects filter changes while open.

No emoji. Never reference position/. No git mutation. Report to .sprint/results/mr-TASK-003-codex.md.
