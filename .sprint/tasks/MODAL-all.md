TASK MODAL - implement the whole full-screen chart analysis modal sprint

You are the SOLE writer of all frontend files for this task. Another engineer is working only on Python files (dashboard.py, src/screening/market_motion_*.py, tests/test_market_motion_*.py): do NOT touch those.

Implement everything in .sprint/SPRINT_PLAN_CHART_MODAL.md (read it and .sprint/council/codex-chart-modal.md fully; its decisions are final, including "replace the inline chart, do not add a second control", the classic-script/global boundary for charts.js, z-index of the toast, destroy-before-recreate, non-collapsing explicit height, optional reference lines via Number.isFinite, focus-return only if the trigger is still attached, modal element outside #content, close in router.route()). That plan lists TASK-001..TASK-006; do them all in dependency order.

FILES YOU MAY TOUCH: static/js/core/price-analysis.js (new), static/js/core/chart-modal.js (new), static/js/core/ui-helpers.js, static/js/core/router.js, static/js/charts.js, static/js/views/positions.js, static/js/views/shortlist.js, static/js/views/market.js, static/css/dashboard.css, templates/dashboard.html, and new/updated tests under tests/js/. Do NOT touch static/js/core/market-motion.js or tests/js/test_market_motion.js (finished, owned by the lead) and do not touch static/js/views/consistency.js or static/js/core/pick-history.js except where an import must be adjusted.

The working tree holds a large body of the user's UNCOMMITTED frontend work (market redesign, news component, consistency badges, sample-scan labels). Build on the CURRENT files exactly as they are on disk; never revert or reformat anything you did not need to change. market.js currently exports scanOptionLabel/pickDefaultScan/buildBuyOpportunityPoints/_showBuySignal etc. that existing tests import: keep every existing export and behaviour, and keep all existing tests in tests/js/ passing (update a test only where the inline-chart removal makes its old assertion obsolete, and say so in the report).

The Buy Opportunity Map scatter (renderMarketScatterChart, #marketScatterChart, buildBuyOpportunityPoints, its card markup) is being replaced later by a different sprint: leave it untouched and working. Its click handler that jumps to a Buy Signals row must keep working.

Also add: `@media (prefers-reduced-motion: reduce)` CSS that removes non-essential animation/transition (row pulse, live dot, spinner, toast, modal open) with `animation-duration/transition-duration: 0.01ms !important`. No emoji anywhere. Plain Node tests in the repo's existing style (see tests/js/*.js) for: price-analysis math (SMA, stats, range slicing incl. short histories/NaN/empty), modal DOM/ARIA structure, focus trap/Escape/backdrop close, focus-return safety, per-view reference lines (Positions entry+stop, Market buy "Scanner stop", Market sell breakdown level, Shortlist none), router closing the modal, and the classic-script boundary (charts.js must not use `export`). Run ALL tests in tests/js/ (`for f in tests/js/*.js; do node "$f"; done`) plus `venv/bin/python -m pytest tests/test_dashboard_jobs.py -q`, and verify the ES-module graph loads: `node --input-type=module -e "..."` importing static/js/dashboard.js with the same DOM stubs the existing tests use.

Report path: .sprint/results/modal-all-codex.md (keep it under 60 lines: files changed, decisions, deviations, test commands + results, risks). Follow every rule in .sprint/tasks/mm-common.md (no git mutation, no position/, no emoji, no network, no LLM calls).
