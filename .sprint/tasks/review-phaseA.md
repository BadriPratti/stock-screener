You are an independent code reviewer (Engineering Council). Do NOT modify any file except your single output file. Tool limits: only cat, grep, rg, find, ls, head, tail, wc, sed, awk, file, pwd. No git/python/node.
Repository: /Users/badripratti/Desktop/stock-screener. NEVER open or list the directory `position/`.

Review the just-built backend for the "persistent buy-opportunity tracker" feature and report REAL defects only (with file:line, a concrete failing scenario, and severity). Do not pad with style nits. Context: .sprint/SPRINT_PLAN_LIVE_CHART.md (Amendments 1 and 2 are the final decisions).
Files: src/screening/market_motion.py (frames + derived tracker), src/screening/intraday_rescore.py + run_intraday_rescore.py (light re-score), run_optimized_scan.py (functions resolve_output_policy, build_market_motion_points, record_market_motion and their call site near the end of main(); the buy-scoring loop that fills scored_non_buys), scripts/ci_push_with_rebase.sh, .github/workflows/daily_screening_git_storage.yml and midday_quick_scan.yml (staging + push steps), dashboard.py (the /api/market-motion* routes and _resolve_scan_path), scripts/backfill_market_motion.py.
Focus on: (1) streak/tier/denominator logic correctness in compute_tracker vs the rules in the plan (unknown != dropped, Friday->Monday consecutive, top50_only coverage, scoring-version boundary); (2) anything in run_optimized_scan.py that could break or delay the REAL daily scan or its email/commit (this is the worst outcome: check exception paths, ordering, memory/time cost of building points for ~2,000 analyses, tracked-ticker lookup cost); (3) the CI push script and workflow YAML: shell correctness (set -euo pipefail, arrays, bash 5 on ubuntu-latest), shallow-clone rebase, whether staged files could be lost, whether `git pull --rebase` could run on a dirty tree in CI, concurrency interplay; (4) intraday_rescore: does it truly avoid writing shared state; can a partial run write a frame; is the market-open guard right; scoring-loop parity with run_optimized_scan.main; (5) API path handling and JSON strictness; (6) anything that violates: no emoji, no reads of position/.
Write your findings to .sprint/reviews/mm-phaseA-antigravity.md, most severe first, max 120 lines. End with a one-line verdict per file.
