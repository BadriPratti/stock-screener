TASK MM-002 - full-scan market-motion frame writer + CI publishing/hardening

FILES YOU MAY TOUCH
- run_optimized_scan.py
- .github/workflows/daily_screening_git_storage.yml
- .github/workflows/midday_quick_scan.yml
- scripts/ci_push_with_rebase.sh (NEW)
- tests/test_scan_run_kind.py (extend), tests/test_workflows_pick_history.py (extend/adjust), NEW tests/test_market_motion_scan.py, NEW tests/test_ci_push_with_rebase.py
Report path: .sprint/results/mm-TASK-002-codex.md

GOAL
The full-universe scan must emit a market-motion frame (see market_motion.build_frame/write_frame) containing EVERY qualified buy signal (not just the top 50) plus an honest outcome row for every previously tracked ticker that did not qualify in this run. Publishing this frame from CI must be safe against push races.

REQUIREMENTS
1. New CLI flag `--record-market-motion` (store_true) on build_arg_parser(). Extend resolve_output_policy(...) to return an additional key `record_market_motion`: True when (run_kind == 'daily-full' OR the flag is given) AND NOT test_mode AND run_kind != 'midday-sample'. Existing keys/behaviour (write_canonical_latest, record_history) must not change; update the truth-table tests. The pick-history ledger must still be written ONLY for `--run-kind daily-full` non-test runs exactly as today (a local run with just the new flag must NOT write pick-history).
2. In main(): after the buy-signal loop, keep the scored-but-not-buy signals (phase 1/2 analyses whose score_buy_signal is_buy is False) in a local dict so outcomes can be built without re-scoring. After save_report and after the existing pick-history call, and BEFORE the email is sent, call a new fail-soft helper `record_market_motion(...)` modelled exactly on record_pick_history (try/except around import+call, logger.error, and print `::warning title=market-motion::frame write failed: <reason>` so it is visible in Actions logs; never raises, never blocks email).
3. Frame content: points = [market_motion.normalize_buy_signal(sig, rank=i) for i, sig in enumerate(buy_signals, 1)] (buy_signals is already sorted by score desc; ALL of them) + for each ticker in market_motion.tracked_tickers(frames loaded via market_motion.load_frames(root)) that is NOT in the buy set: outcome_point using the retained scored signal (-> not_qualified), or phase-only for phase 3/4 or phase-1 analyses (-> not_scored with phase), or, if that ticker has no analysis in this run (filtered by price/volume, fetch failed), outcome_point(ticker, error="not_analyzed"). session_date = market_motion.session_date_from_bar(<last index value of processor.spy_data>); if spy data is missing/empty, skip recording with a warning (never fall back to wall-clock silently). generated_at = now UTC. scope = {"mode": "full_universe", "requested": len(tickers), "analyzed": results.get('total_analyzed'), "completed": True}; coverage complete_for_scope; regime = {"should_generate_buys": signal_rec['should_generate_buys'], "source_run_id": None}; provenance "live" when run_kind == 'daily-full' else "local"; source from GITHUB_SHA/GITHUB_RUN_ID/GITHUB_WORKFLOW like record_pick_history. If total_analyzed is 0/None the run is broken: skip with a warning (do not record an empty market observation). A legitimate zero-buy run (regime gate off, or none qualified) with analyzed > 0 IS recorded.
4. Never record test-mode/sample runs. Never write outside data/market_motion/. Never change what is written to top20_latest.json, shortlist_latest.json, the text report, pick-history, or the email.
5. daily workflow: (a) add a "Stage market motion" step mirroring the existing "Stage pick history" step (continue-on-error, `git add data/market_motion/`, ::warning on failure, no `|| true` swallowing); (b) add `data/market_motion/` to the artifact upload paths; (c) `timeout-minutes: 120` and per-workflow concurrency groups must be preserved; (d) replace the final push (ad-m/github-push-action) in BOTH workflows with a step that runs the new script scripts/ci_push_with_rebase.sh. Keep the same `if: steps.commit.outputs.has_changes == 'true'` condition and `permissions: contents: write`.
6. scripts/ci_push_with_rebase.sh (bash, `set -euo pipefail`): args: <branch> [max_attempts=4]. Loop: `git pull --rebase --autostash=false origin <branch>` (fail => `git rebase --abort || true`, sleep, retry) then `git push origin HEAD:<branch>`; on success exit 0; after max attempts print `::error title=push-failed::...` and exit 1. Must work in an `actions/checkout` fetch-depth:1 (shallow) clone. Backoff sleep must be overridable (env PUSH_RETRY_SLEEP, default 5) so tests are fast. The reason this exists: the user's local dashboard now also pushes to main during the ~64 minute daily run, and the old push step had no rebase, so a race would silently lose the day's committed scan.
7. Tests (all offline): truth table for resolve_output_policy; a stubbed-processor test of the frame builder (extract the frame-building logic into a small pure function such as `build_market_motion_points(...)` so it is unit-testable without running a scan): every buy present (not just 50: test with 120), tracked-but-dropped outcomes for each of the states (not_qualified, not_scored phase 3, error not_analyzed), dedupe, zero-buy-with-analyzed>0 records, analyzed==0 skips, missing SPY data skips, test-mode/midday never record, local-with-flag records provenance "local" and does NOT call the pick-history writer, fail-soft (writer raising does not propagate and prints the ::warning). For the push script: pytest with tmp_path bare repo + a `--depth 1` shallow clone (file:// URL) proving (i) a clean push works, (ii) a push after the remote advanced with a NON-conflicting commit succeeds via rebase, (iii) after max attempts of a permanently failing push it exits non-zero, (iv) it never runs `git push --force`. Use the working git binary (try shutil.which('git') then /Library/Developer/CommandLineTools/usr/bin/git, skip if neither runs, as tests/test_workflows_pick_history.py does). Also assert both workflow YAMLs still parse, keep their concurrency groups, and no longer reference ad-m/github-push-action.
