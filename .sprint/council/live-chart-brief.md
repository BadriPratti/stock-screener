You are participating in an Engineering Council. DO NOT modify, create, or delete any file in the repository except the single output file named at the bottom. Do not run git commands, python, node, or network calls.

Repository: /Users/badripratti/Desktop/stock-screener  (a local Flask + pywebview "Stock Screener" dashboard; native ES modules, no build step; Chart.js 4.4.7 vendored)
NEVER open, list, or reference the directory `position/` (real account data).

## The user's request (their words)
"the chart of buy opportunity should also include this like realistically that graph should be packed to the brim because those stocks that it viewed before should still be there realistically we should do this stock scan every 2 hours and everything simultaneously moves in motion"

Interpretation to test (challenge it if wrong): the Market tab "Buy Opportunity Map" scatter (X = relative strength vs SPY, Y = scanner score % of max) should (a) keep every stock seen in earlier scans on the chart instead of only the latest scan (memory), (b) animate/move as new scans land - points moving between positions, entering and leaving, (c) be fed by a scan that runs every ~2 hours instead of once a day, so the motion is real.

## Read these first (all inside the repo)
- .sprint/council/live-chart-data/FACTS.md  (verified facts: read this fully; it contains numbers that change the design)
- .sprint/council/live-chart-data/buy_signal_facts.txt and sample_buy_signal.json
- static/js/views/market.js (buildBuyOpportunityPoints, _buyOpportunityChartCardHTML, loadMarketScan), static/js/charts.js (renderMarketScatterChart, _destroyChart, ENTRY_QUALITY_COLORS), static/css/dashboard.css
- dashboard.py (parse_scan_file, /api/scans, /api/scan, /api/pick-history), src/screening/pick_history.py (ledger design and its semantics)
- run_optimized_scan.py (save_report top-50 cap, resolve_output_policy, record_pick_history, main flow), .github/workflows/daily_screening_git_storage.yml and midday_quick_scan.yml
- .sprint/SPRINT_PLAN_CHART_MODAL.md (an approved-in-principle but unbuilt plan touching the same files) and .sprint/SPRINT_PLAN.md (the pick-history plan that was just built)

## Questions the council must answer with repository evidence
1. DATA: The chart currently plots at most 50 dots per scan because the text report only details the top 50 buy signals (364-479 exist). What should each plotted point be, and where does the data come from so that history is real? Options include: a new machine-readable sidecar written by the scanner (all buy signals or a capped/filtered set), extending the pick-history ledger with a buy_signals list, or re-parsing old text reports. How do we backfill from the 5 existing full-scan reports in git? What is the honest handling of stocks that were seen before but are NOT in the latest scan (dropped out)? Note the Y axis only spans 81-93% in the top 50 - is the axis even useful, and should the axes/encodings change?
2. VISUAL DESIGN: "packed to the brim" without becoming unreadable. Age fade, trails/tails per ticker, dropped-out styling, size/color encodings, label policy for crowded points, decimation limits, and the point budget Chart.js can animate smoothly on canvas. Be concrete (e.g. N points, alpha curve, trail length).
3. MOTION: How to animate "everything moves simultaneously" as a time-scrubbable replay (play/pause/step/scrub slider over recorded scans, tween positions between snapshots) in Chart.js 4.4.7 without a build step. Say whether to use Chart.js built-in animations, a custom plugin drawing on the canvas, or requestAnimationFrame interpolation over a dataset; how to handle enter/exit tweens; how to honour prefers-reduced-motion (currently unhandled anywhere in dashboard.css); how it must coexist with charts.js being a classic script and with _destroyChart lifecycle. Also how a live update (new scan lands during an open session) should animate.
4. INTRADAY LEDGER: The ledger keys on one snapshot per ET calendar date and its whole semantics (streak = consecutive recorded sessions, "N scans" badges, denominator = min(window, sessions), idempotent same-date replace) assume one snapshot per day. Design the granularity for intraday snapshots WITHOUT breaking the daily semantics/tests: a separate run_kind/directory, per-run filenames, how daily consistency badges stay defined per day (12 intraday scans must not inflate "N scans"), how the chart consumes the intraday series, retention/size limits, and git-history growth.
5. SCHEDULING (the user must approve this; do not assume): "every 2 hours". A full-universe scan takes ~63 minutes, the daily scan also runs LLM agents (Anthropic API cost) and sends emails; GitHub cron is best-effort and observed to start 4-5 hours late; the repo is public (free runner minutes). Recommend a concrete, cost-aware cadence and architecture: what an intraday run should actually compute (a light re-score of the ~50-500 tracked candidates vs another full universe scan), whether it uses LLM agents (no?), email (no?), market-hours-only cron in UTC with DST caveats, workflow_dispatch, concurrency groups and overlap with the daily run, push contention / rebase-before-push, artifact/commit size, and whether a LOCAL scheduler inside the dashboard (the Mac) is a better fit than GitHub cron for "every 2 hours". Give the numbers (runs/day, minutes/run, commits/day, bytes/day/year).
6. SEQUENCING/CONFLICTS: How does this sprint interact with the unbuilt full-screen chart modal plan and the market redesign (shared files: market.js, charts.js, dashboard.css, ui-helpers.js)? Propose task ordering and file ownership so no two writers ever touch the same file concurrently.
7. RISKS/EDGE CASES/TESTS: weekend gaps and market holidays in the time axis, partial/failed runs, same-ticker duplicates, scoring_version changes, NaN/None fields, backwards-compat of existing API consumers, performance with 2,000+ points, stale-response races, accessibility (canvas needs a text/table alternative), no-emoji rule, tests in the repo's plain-Node/pytest style.
8. Anything the other engineers will likely miss, and any part of the user's request you think is a bad idea or should be reframed.

Do not simply agree with the user's framing or with another agent. Base every claim on repository evidence (cite file paths and line numbers). Be concrete: name functions, files, JSON shapes, and numbers. Give a recommended task breakdown (TASK ids, owner suggestion Claude/Codex/Antigravity, dependencies, files touched, acceptance criteria) at the end.
