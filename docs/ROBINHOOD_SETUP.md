# Robinhood Position Fetcher (superseded)

This project now uses Fidelity — see [Fidelity Setup](FIDELITY_SETUP.md) for the current position-management docs.

`src/data/robinhood_positions.py` (the old `robin_stocks`-based login integration this doc used to describe) is left in the codebase unused, in case Robinhood support is needed again later, but nothing currently calls it.
